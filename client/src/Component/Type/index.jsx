import React from 'react';
import PropTypes from 'prop-types';

Type.propTypes = {
    
};

function Type(props) {
    return (
        <div>
            Type
        </div>
    );
}

export default Type;